kind: pipeline
name: default

platform:
  os: linux
  arch: arm64

steps:
  - name: install dependencies
    image: node
    commands:
      - npm install

  - name: lint check
    image: node
    commands:
      - npm run lint
